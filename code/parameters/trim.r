read_length <- 300 # read length from sequencing run.
amplicon_length <- 425 # bp of amplicon in metabarcoding.
trunc_f <- 270 # truncation length for forward reads.
trunc_r <- 185 # truncation length for reverse reads.
expected_errors <- c(3, 6) #maximum expected_errors for foward and reverse reads
